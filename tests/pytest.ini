[pytest]
; Studio uses PyQt5. If PySide6 is also installed in the environment running
; pytest, pytest-qt's auto-detection prefers PySide6 for its qapp/qtbot
; fixtures, and loading both Qt bindings' native frameworks in one process
; segfaults. Pin pytest-qt to PyQt5 to match the app.
qt_api = pyqt5
